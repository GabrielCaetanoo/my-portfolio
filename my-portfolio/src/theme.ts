import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: '#E88D67',
        },
        secondary: {
            main: '#F3F7EC',
        },
    },
});

export default theme;